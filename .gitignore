# --- Secrets / production config (NEVER commit) ---
inc/config.prod.php
inc/config.prod.php.backup

# --- Internal handoff doc (admin login + server paths — never publish/deploy) ---
PROJECT-HANDOFF.md

# --- OS / editor cruft ---
Thumbs.db
Desktop.ini
.DS_Store
*.swp
*~

# --- Logs / temp ---
*.log
error_log
php_error_log

# --- Local deploy artifacts ---
/deploy/secrets.*
.deploy.local
deploy-*.zip
*_db_export.sql
*.dump.sql

EXCEL-PRODUCTS.md
items-images/
*.xlsx

# Internal status/handoff doc (read locally by a new chat)
PROJECT-STATUS.md
PRODUCT-IMPORT-GUIDE.md
NEXT-STEPS.md

# dev mail dumps
/storage/

# PFDC import source (keep out of git)
Updated PFDC full e-comm catalogue.xlsx
vichy-LRP-CeraVe*.xlsx

# local handoff runbook (do not publish)
HANDOFF-CATALOG-DEPLOY.md

# product audit working docs (local only)
WELL-PHARMACY-*.csv
MESSAGE-TO-CLIENT*.md

# Beesline brand package (local source — not for git)
wetransfer_beesline-packshots_2026-07-21_1234/

# cPanel deploy runbook (local only — internal)
CPANEL-DEPLOY.md
